# context-propagation-go-usage

Agent-package with instructions and a skill for propagating request
context (request-id, tenant, user, tracing) through Go microservices —
HTTP handlers, outbound calls, and background work. The agent applies
it automatically when working with Go code in a Qubership service.
